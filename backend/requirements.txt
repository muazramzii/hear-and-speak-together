# ---- Core framework ----
Django>=5.1,<6.0
djangorestframework>=3.15,<4.0

# ---- Authentication (JWT) ----
djangorestframework-simplejwt>=5.3,<6.0

# ---- Database (PostgreSQL, psycopg 3) ----
psycopg[binary]>=3.2,<4.0
dj-database-url>=2.2,<3.0

# ---- API support ----
django-cors-headers>=4.4,<5.0
django-filter>=24.3

# ---- Configuration / assets ----
python-dotenv>=1.0,<2.0
Pillow>=10.4

# ---- Production serving ----
# `manage.py runserver` is a development tool and must not be used to deploy.
gunicorn>=23.0,<24.0
whitenoise>=6.7,<7.0

# ---- Speech recognition (self-hosted Whisper, CTranslate2 reimplementation) ----
# Only loaded when SPEECH_PROVIDER=whisper; tests use the mock service.
faster-whisper>=1.0,<2.0

# ---- Pronunciation scoring (local, deterministic, no network calls) ----
# g2p_en: English grapheme-to-phoneme (CMU dict + trained model).
# panphon: articulatory feature vectors for phonetic-distance scoring.
# nltk: g2p_en's tokenizer/POS-tagger dependency (averaged_perceptron_tagger, cmudict).
g2p_en>=2.1,<3.0
panphon>=0.20,<1.0
nltk>=3.9,<4.0

# ---- Optional LLM feedback (Gemini / OpenAI over plain HTTP) ----
requests>=2.32,<3.0
