<?xml version="1.0" encoding="utf-8"?>
<!--
    Android blocks plain-HTTP traffic by default from API 28 onwards.

    Development runs Django over http:// on the developer's machine, so
    cleartext is permitted for those hosts ONLY. Every other destination,
    including the production API, must use HTTPS.

      10.0.2.2  - the host machine, as seen from the Android emulator
      localhost / 127.0.0.1 - on-device loopback
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
