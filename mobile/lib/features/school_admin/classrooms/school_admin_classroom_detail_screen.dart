import 'package:flutter/material.dart';

class SchoolAdminClassroomDetailScreen extends StatelessWidget {
  const SchoolAdminClassroomDetailScreen({super.key, required this.classroomId});

  final int classroomId;

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: SizedBox.shrink());
  }
}
